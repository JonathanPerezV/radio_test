import 'package:flutter/material.dart';

class Modals {
  void showLoginCredentias(context) {
    showModalBottomSheet(
        context: context,
        builder: (builder) {
          return Column(
            mainAxisSize: MainAxisSize.min,
            children: [],
          );
        });
  }
}
